# Nginx basic auth for protected subdomains (logs.hireadda.in)
# Generate on VPS with: htpasswd -Bn admin
# Then paste the output here, or run:
#   docker run --rm httpd:alpine htpasswd -Bn admin > nginx/htpasswd
#
# Format: username:bcrypt_hash
# This placeholder will be overwritten during VPS setup.
