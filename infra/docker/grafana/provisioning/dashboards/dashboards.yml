# Dashboard provisioning — loads JSON files from /var/lib/grafana/dashboards
apiVersion: 1
providers:
  - name: 'Talent Bridge'
    orgId: 1
    folder: 'Talent Bridge'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 60
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
